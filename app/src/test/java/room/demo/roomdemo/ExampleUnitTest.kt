package room.demo.roomdemo

import org.junit.Test

import org.junit.Assert.*

/**
 * Pengujian unit lokal, yang akan dieksekusi pada mesin pengembangan (host).
 *
 * 
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
